/*import { ComponentFixture, TestBed } from '@angular/core/testing';
import { BaseFormUser } from './base-form-user';

describe('RoutineGeneratedComponent', () => {
  let component: BaseFormUser;
  let fixture: ComponentFixture<BaseFormUser>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ BaseFormUser ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(BaseFormUser);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });
});*/
