/*import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ExerciseInformationMobileComponent } from './exercise-information-mobile.component';

describe('ExerciseInformationMobileComponent', () => {
  let component: ExerciseInformationMobileComponent;
  let fixture: ComponentFixture<ExerciseInformationMobileComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ ExerciseInformationMobileComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(ExerciseInformationMobileComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
*/